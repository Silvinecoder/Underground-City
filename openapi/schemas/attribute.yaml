openapi: 3.1.0
components:
  schemas:
    Attribute:
      type: object
      properties:
        attribute_uuid:
          type: string
          format: uuid
        attribute_type:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time